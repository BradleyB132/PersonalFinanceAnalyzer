# Project README (Generated)

This repository contains a minimal implementation of PersonalFinanceAnalyzer.

Structure:
- `src/` - application code (Streamlit app and DB helpers)
- `db/` - SQL schema and seed data
- `docs/` - design docs and user stories

To run: see `README_RUN.md`.

Security:
- Database connection string is stored in `.env` (do not commit secrets in real projects)

Testing:
- No tests included in this minimal implementation
